# Domain docs

This workspace uses a single-context domain-document layout.

Before exploring a topic, read root `CONTEXT.md` if present and the relevant
decisions in `docs/adr/`. If either location is absent, proceed silently.

Use terms defined by `CONTEXT.md`. Surface any conflict with an existing ADR
instead of silently overriding it.
